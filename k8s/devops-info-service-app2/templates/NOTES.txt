1. Get the release status:
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}

2. Inspect the rendered Service:
   kubectl get svc {{ include "devops-info-service-app2.fullname" . }} -n {{ .Release.Namespace }}

3. For local verification through port-forward:
   kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "devops-info-service-app2.fullname" . }} 8081:{{ .Values.service.port }}
   curl http://127.0.0.1:8081/health
   curl http://127.0.0.1:8081/ready
